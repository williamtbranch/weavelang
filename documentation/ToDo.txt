
================================================================================
Project Plan: WeaveLang V9 - The Common Pool Architecture
Last Updated: (Date of this session)
================================================================================

### Primary Objectives & Guiding Principles

- **Goal:** Implement a scalable, cost-effective data pipeline using a "Common Pool"
  for reusable, pre-processed linguistic data.
- **Architecture:** A two-part system. Part 1 is the "Pool Manager" which generates
  and caches reusable, language-specific data. Part 2 is the "Pipeline Runner"
  which consumes pooled data to generate pair-specific artifacts.
- **Golden Rule:** Expensive, reusable operations are done ONCE and cached. Data integrity
  between the Python producer and Rust consumer is paramount.

---
### Master Checklist
---

#### Phase 1: Foundational Tooling & V9 Pipeline (Complete)

[x] 1.1: Implement Common Pool Manager for reusable assets.
[x] 1.2: Refactor pipeline into new V9 stages (Assemble, GenSimple, FinalizeSimple, etc.).
[x] 1.3: Implement multi-word to multi-word phrase mapping (`GeneratePhraseMap`).
[x] 1.4: Implement `base` tier refactoring to create "virtual tokens" (`ApplyPhraseMappings`).
[x] 1.5: Successfully debugged and completed a full end-to-end run of the Python pipeline.

#### Phase 2: Rust Engine Integration & Debugging (Current Phase)

[x] 2.1: **Fix `simple_target` Text Reconstruction Bug:**
    - [x] Write a failing unit test in `test_data_reconstruction.py` that asserts `full_text` is correctly reconstructed from segments.
    - [x]  **CURRENT TASK:** Modify `FinalizeSimpleTarget` (Stage 3) to correctly join segment `text` properties to form the `full_text` with proper spacing.

[x] 2.2: **Add `di` keys to `simpler_advanced_target`:**
    - [x] **Problem:** This tier is missing `di` keys, making the inverse diglot map unusable.
    - [x] Create a new stage, `FinalizeSimplerAdvTarget` (Stage 4), to run after `GenerateSimpleTarget` and before `GeneratePhraseMap`.
    - [x] Write a failing unit test in `test_data_reconstruction.py` that asserts `di` keys are present and sequential.
    - [x] Implement the logic in the new `FinalizeSimplerAdvTarget` stage to tokenize and add `di` keys.
    - [x] Re-number all subsequent stages in the orchestrator and their class definitions.

[x] 2.3: **Fix `inverse_diglot_map` Lemma Population:**
    - [x] **Problem:** The map contains empty lemmas due to the missing `di` keys.
    - [x] Once task 2.2 is complete, verify that the existing logic in `FinalizeMappings` correctly populates the lemmas. No new code should be needed, but verification is critical.

#### Phase 3: Final Validation & Refinement

[x] 3.1: **Refactor Rust `core_algo` for Phrase-Weaving:**
    - [x] Modify the `L1 Simple Hybrid` logic to iterate through the phrase-atoms in `simple_target_to_base_diglot` instead of single English words.
    - [x] Ensure the new failing `weavetest` cases pass after the refactor.

[x] 3.2: **Refactor `GenerateInverseDiglotMap`:**
    - [x] Once the entire system is stable, refactor Stage 6 to support multi-word inverse mappings, mirroring the successful design of Stage 4/5.

[x] 3.3: Perform a final, full-corpus run and analyze the quality of the generated text at all levels. Tune Rust engine parameters as needed.

[x] #### Phase 4: Replicate multi-word diglot functionality from L1 to L0 inverse diglot
#### Phase 5: Finalize level system
#### Phase 6: Work in level cap into Simpler Sentences using LLM feedback
#### Phase 7: Implement stretch system for the engine
    - Book starts at level n and ends at either n+1 or 2% increased vocab based on book text - not frequency list
    - These start and end vocab number calculations are saved into the json file itself for use by the engine
#### Phase 8: Python pool stage add type of chapter marker set for every estimated 20 minutes of read time for publication equalized to keep all chapters approximately the same size.
#### Phase 9: Develop TTS output filing system for auto generated complete all level books with 20 minute segments marked.
#### Phase 10: Post processing LLM call system to create and save illustrations for each 20 minute section.
#### Phase 11: Auto generation of book summaries for Youtube.
#### Phase 12: Expand TTS to create audio for 20 minute chapters + full length version
#### Phase 13: Create Video Generator with 1 frame per minute or worst cast 1 per second for books.
#### Phase 14: If possible Generate Youtube posting script.

#### Phase 15: Implement "Validate-at-Source" for All LLM Stages

- [ ] **Goal:** Prevent expensive re-runs by ensuring all data from LLM calls is validated for structural and semantic integrity *immediately* after it is received, before being saved. This moves validation from the end of the pipeline to the point of data creation.

- [ ] **Task 1: Refactor `GenerateSimpleTarget` (Stage 2):**
    - [ ] Add a post-LLM validation step.
    - [ ] **Check:** Does the number of returned translations match the number of segments sent?
    - [ ] **Action on Fail:** Halt the pipeline with a clear error indicating an incomplete LLM response for the batch.

- [ ] **Task 2: Refactor `GenerateInverseDiglotMap` (Stage 7):**
    - [ ] The existing `validate_llm_phrase_map_content` check is good. Ensure it is robust and remains in the `run` method.

- [ ] **Task 3: Create a `prompt_validator.py` Module:**
    - [ ] **Goal:** Centralize validation logic that is specific to prompt outputs.
    - [ ] Move `_validate_llm_map`, `_rejoin_llm_contractions`, etc., from `phrase_mapper_helpers.py` into this new, dedicated file. This improves separation of concerns.
    - [ ] Update `GeneratePhraseMap` and `GenerateInverseDiglotMap` to import from this new module.

- [ ] **Task 4: Implement a Retry-with-Correction Mechanism (Advanced):**
    - [ ] **Goal:** For validation failures, attempt to automatically correct the prompt and retry the LLM call for the failed batch *once*.
    - [ ] **Logic:** If `_validate_llm_map` fails, `run_llm_batch_job` could be called again with an augmented user prompt like: `"Your last response was invalid. Please follow the rules exactly. Here is the data again..."`
    - [ ] This adds resilience and can salvage expensive batches without requiring a full manual restart.