================================================================================
Project Plan: WeaveLang V10 - The Three-Tier Simplification Architecture
Last Updated: (Date of this session)
================================================================================

### Primary Objectives & Guiding Principles

- **Goal:** Implement a smoother, more pedagogically sound learning gradient by refactoring the pipeline and engine to support a three-tier simplification model (Simple, Basic, Moderate).
- **Architecture:** The Python pipeline will produce these three distinct tiers of simplified text. The Rust engine will intelligently weave them together, prioritizing more natural prose as the learner's vocabulary grows.
- **Golden Rule:** The "jarring" stylistic gap must be eliminated. The learner's journey should be a gradual ascent from functional, deconstructed language to fluent, natural prose, before reaching the final, literary text.

---
### Master Checklist
---

#### Phase 1: Foundational Tooling & Prompt Engineering (Complete)

[x] 1.1: Implement a robust, quantitative prompt rating system (`rate_prompts.py`).
[x] 1.2: Fix critical bugs in text normalization and data parsing.
[x] 1.3: Engineer and validate a "Simple" (NSM-Lite) prompt (`test04`) that successfully minimizes vocabulary complexity.
[x] 1.4: Engineer and validate a "Moderate" (Natural) prompt (`test01`) that balances simplicity and fluency.
[x] 1.5: Engineer and validate a "Basic" (Intermediate) prompt (`test07`) that successfully bridges the stylistic gap between Simple and Moderate.

#### Phase 2: Python Pipeline Refactor (Current Phase)

- [ ] **Task 2.1: Refactor the PoolManager for Multi-Tier Generation**
    - [ ] Modify `pool_manager.py` to generate three separate derived text assets for the target language. The `_get_or_create_sim_json` method will be the template.
    - [ ] Create `_get_or_create_mod_json` which will use the `simplify_segments_moderate.txt` prompt.
    - [ ] Create `_get_or_create_bas_json` which will use the `simplify_segments_basic.txt` prompt.
    - [ ] The existing `_get_or_create_sim_json` will be updated to use the `simplify_segments_simple.txt` prompt.

- [ ] **Task 2.2: Update Stage 1 (AssembleTiers) to Handle New Tiers**
    - [ ] Modify `assemble_tiers.py` to load the new `.mod.json` and `.bas.json` files from the common pool.
    - [ ] The stage will now assemble a JSON structure containing five tiers: `base`, `advanced_target`, `moderate_target`, `basic_target`, and `simple_target`.

- [ ] **Task 2.3: Refactor the Final Stage (FinalizeBook) for Lean Output**
    - [ ] Modify `finalize_book.py` to perform selective cleanup.
    - [ ] For the `advanced_target`, `moderate_target`, and `basic_target` tiers:
        - [ ] Strip the `tokenized_text` key from each segment after lemmatization is complete, leaving only `seg_id`, `text`, and `lemmas`.
    - [ ] For the `simple_target` tier, which is used for diglotting, all existing keys (`tokenized_text`, etc.) should be preserved.
    - [ ] This ensures the final JSON is as lean as possible for the Rust engine.

#### Phase 3: Rust Engine Refactor

- [ ] **Task 3.1: Update JSON Parsing and Numerical Types**
    - [ ] In `json_types.rs`, add `moderate_target` and `basic_target` to the data structures.
    - [ ] In `numerical_types.rs`, update the `NumericalAdvSegmentBundle` struct to hold the text and lemma IDs for the `Moderate` and `Basic` tiers.
    - [ ] Update `preprocessor.rs` to correctly map the new JSON tiers to the updated numerical structs.

- [ ] **Task 3.2: Implement the New L0 Weaving Logic**
    - [ ] In `core_algo.rs`, modify the `try_build_advanced_weave` function.
    - [ ] Implement the new four-step fallback logic for each segment:
        1. Attempt to use `Advanced` text.
        2. If fails, attempt to use `Moderate` text.
        3. If fails, attempt to use `Basic` text.
        4. If fails, attempt to use `Simple` text.
        5. If all fail, fall back to the Inverse Diglot mechanism using the `Simple` text as the base.

- [ ] **Task 3.3: Update the WeaveLang Test Suite**
    - [ ] Modify the `generation_tests.pest` grammar to support defining `Basic` and `Moderate` segments within the DSL.
    - [ ] Update `weavetest_parser.rs` to parse this new structure.
    - [ ] Create new test cases in `generation_tests.weavetest` that specifically assert the correct fallback behavior of the new four-tier weave. For example, create a test where a learner knows the "Basic" words but not the "Moderate" words, and assert that the "Basic" text is chosen.

#### Phase 4: Final Validation and Future Work

- [ ] **Task 4.1: Perform a Full Corpus Run:** Execute a full run of the new Python pipeline and Rust engine on a book like `LesMis`.
- [ ] **Task 4.2: Analyze the New Output:**
    - [ ] Qualitatively review the generated TTS text at various levels to confirm the "jarring" effect is gone.
    - [ ] Analyze the new `corpus_analysis_log.txt` to see the distribution between the four segment types (`Adv`, `Mod`, `Bas`, `Sim`).
- [ ] **Task 4.3: Plan the Per-Tier Vocabulary Sweep:** The current plan focuses on getting the data structure and weaving logic in place. The next major architectural step will be to implement the per-tier vocabulary exhaustion model we discussed. This will be WeaveLang V11.

#### Phase 2: Python Pipeline Refactor (Current Phase)

- [ ] **Task 2.1: Refactor the PoolManager for Multi-Tier Generation**
    - [ ] Modify `pool_manager.py` to generate three separate derived text assets for the target language. The `_get_or_create_sim_json` method will be the template.
    - [ ] Create `_get_or_create_mod_json` which will use the `simplify_segments_moderate.txt` prompt.
    - [ ] Create `_get_or_create_bas_json` which will use the `simplify_segments_basic.txt` prompt.
    - [ ] The existing `_get_or_create_sim_json` will be updated to use the `simplify_segments_simple.txt` prompt.

- [ ] **Task 2.2: Update Stage 1 (AssembleTiers) to Handle New Tiers**
    - [ ] Modify `assemble_tiers.py` to load the new `.mod.json` and `.bas.json` files from the common pool.
    - [ ] The stage will now assemble a JSON structure containing five tiers: `base`, `advanced_target`, `moderate_target`, `basic_target`, and `simple_target`.

- [ ] **Task 2.3: Fix the "Empty Mapping" Bug in the Phrase Mapper**
    - [ ] **Problem:** The current phrase mapper (`apply_phrase_mappings.py`) can generate invalid diglot map entries for phrases it fails to align, such as `[ 14, [], "", true, 3 ]`. This causes words to disappear from the final L1 text.
    - [ ] **Solution:** Modify the logic in `phrase_mapper_helpers.py`. When a multi-word "semantic atom" is invalidated (e.g., for crossing a segment boundary or containing punctuation), it must be correctly broken down into its constituent single-word tokens, and each token must be given a `NO_SUB`, non-viable mapping.

- [ ] **Task 2.4: Implement a New Semantic Validation Stage (Stage 4.5)**
    - [ ] **Problem:** LLMs can produce intermittently "shifted" or nonsensical phrase maps that are syntactically valid but semantically wrong. For example, in `LesMis` sentence `S18`, the LLM incorrectly mapped English `talk` to Spanish `creó`. Our current DP aligner is too robust and "self-heals" this, hiding the underlying data corruption.
    - [ ] **Solution:** Create a new pipeline stage, `ValidatePhraseMappings`, that runs after `ApplyPhraseMappings`.
    - [ ] This stage will use a free, local sentence-transformer model (e.g., `paraphrase-multilingual-MiniLM-L12-v2`) to perform cross-lingual semantic validation.
    - [ ] For each English phrase and its mapped Spanish phrase, it will calculate the cosine similarity of their vector embeddings.
    - [ ] **Validation Rule:** If a sentence contains a significant number of mappings with a similarity score below a defined threshold (e.g., < 0.3), the entire sentence will be flagged with a status like `RETRY_SEMANTIC_FAIL`.
    - [ ] The main pipeline orchestrator will be updated to handle this `RETRY` status, forcing the LLM to re-generate the failed sentence on the next run. This creates a self-correcting loop that guarantees the semantic integrity of our phrase maps.

- [ ] **Task 2.5: Refactor the Final Stage (FinalizeBook) for Lean Output**
    - [ ] Modify `finalize_book.py` to perform selective cleanup.
    - [ ] For the `advanced_target`, `moderate_target`, and `basic_target` tiers:
        - [ ] Strip the `tokenized_text` key from each segment after lemmatization is complete, leaving only `seg_id`, `text`, and `lemmas`.
    - [ ] For the `simple_target` tier, which is used for diglotting, all existing keys (`tokenized_text`, etc.) should be preserved.
    - [ ] This ensures the final JSON is as lean as possible for the Rust engine.

    Next Task: After this run, our next high-priority task must be to debug and fix the Stanza segmenter. We need to create a new, targeted regression test in llm2books/tests/test_segmentation.py using the sentence Con un movimiento de su mano despejó un espacio vacío al frente entre las cosas del desayuno, e se inclinó hacia adelante para mirar con más comodidad. and assert that it does NOT produce a single-word final segment. Then, we will fix the segmenter code to make that test pass.

    TODO: Refactor the LLMSegmenter to perform "Boundary Validation" instead of "Text Validation".
The new process will use the LLM's output to propose cut points.
It will validate these cut points by comparing word counts and boundary-word lemmas against a ground-truth tokenization of the original source text.
If validated, the final segments will be reconstructed by slicing the ground-truth tokens, ensuring 100% fidelity.
This will make the system immune to minor grammatical/orthographic edits made by the LLM.