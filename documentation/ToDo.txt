================================================================================
Project Plan: WeaveLang V9 - The Common Pool Architecture
Last Updated: (Date of this session)
================================================================================

### Primary Objectives & Guiding Principles

- **Goal:** Implement a scalable, cost-effective data pipeline using a "Common Pool"
  for reusable, pre-processed linguistic data.
- **Architecture:** A two-part system. Part 1 is the "Pool Manager" which generates
  and caches reusable, language-specific data. Part 2 is the "Pipeline Runner"
  which consumes pooled data to generate pair-specific artifacts.
- **Golden Rule:** Expensive, reusable operations are done ONCE and cached.

---
### Master Checklist
---

#### Phase 1: Foundational Tooling (Complete)

[x] 1.1: Set up asset structure.
[x] 1.2: Implement and test `build_language_config`.
[x] 1.3: Implement and test `load_prompt_template`.
[x] 1.4: Implement and test core validator functions.
[x] 1.5: [REMOVED] `split_boundary` logic is now obsolete.
[x] 1.6: Successfully integrated Stanza for constituency-based segmentation.

#### Phase 2: Implement the Common Pool Manager (Complete)

[x] 2.1: Created `llm2books/pool_manager.py`.
[x] 2.2: Implemented `pool_manager.generate_std_file`.
[x] 2.3: Implemented `pool_manager.generate_sim_file`.
[x] 2.4: Implemented the main `pool_manager.get_book_resources` function.

#### Phase 3: Refactor the Pipeline to Use the Pool Manager

[x] 3.1: **Stage 1 (`AssembleTiers`):**
    - [x] **CURRENT TASK:** Create the class `llm2books/stages/assemble_tiers.py`.
    - [x] Implement the logic to read the pool files and create `stage1.json`.
    - [ ] Write integration tests for this assembly process.

[ ] 3.2: **Stage 2 (`GenerateSimpleTarget`):**
    - [ ] Create placeholder class and integrate.
    - [ ] Write integration test.
    - [ ] Implement stage logic (LLM stage).

[ ] 3.3: **Stage 3 (`FinalizeSimpleTarget`):**
    - [ ] Create placeholder class and integrate.
    - [ ] Write integration test.
    - [ ] Implement stage logic (SpaCy stage).

[ ] 3.4: **Stage 4 (`GenerateDiglotMap`):**
    - [ ] Create placeholder class and integrate.
    - [ ] Write integration test.
    - [ ] Implement stage logic (LLM stage).

[ ] 3.5: **Stage 5 (`FinalizeDiglotMap`):**
    - [ ] Create placeholder class and integrate.
    - [ ] Write integration test.
    - [ ] Implement stage logic (SpaCy stage).

[ ] 3.6: **Stage 6 (`FinalizeBook`):**
    - [ ] Create placeholder class and integrate.
    - [ ] Write integration test.
    - [ ] Implement final cleanup logic.

#### Phase 4: Final Integration & Validation

[ ] 4.1: Refactor `orchestrate_pipeline.py` to run the full, lean pipeline.
[ ] 4.2: Delete all obsolete stage files from the old design.
[ ] 4.3: Perform a full pipeline run and verify the final `library/*.json` output.