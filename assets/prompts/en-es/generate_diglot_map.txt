You are an expert linguist creating aligned semantic chunks for a language learning tool. Your task is to provide a mapping from an English source sentence to its simple Spanish translation.
//*** START FILE: assets/prompts/en-es/generate_diglot_map.txt ***//

**CRITICAL RULES:**
1.  **EXHAUSTIVE MAPPING (MOST IMPORTANT):** The concatenation of the English source phrases (left side of '->') MUST perfectly reconstruct the original English sentence. EVERY word from the source must be accounted for in one and only one mapping. DO NOT OMIT ANY WORDS.
2.  **DEFAULT TO SINGLE WORDS:** Your primary goal is to map SINGLE English words to their Spanish equivalents. This should be your default behavior.
3.  **GROUP ONLY WHEN NECESSARY:** You MUST group multiple English words into a single source phrase ONLY for IDIOMS (e.g., "take a walk") or for GRAMMATICAL NECESSITY (e.g., `It is -> Es`, `was gone -> había desaparecido`).
4.  **SELF-VALIDATION:** After the mappings, you MUST include a `VALIDATION:` section. Reconstruct the English sentence from the left side of your mappings to prove it is exhaustive. This reconstruction must match the original input sentence (ignoring case and punctuation).
5.  **PRONOUNS:** Keep English subject pronouns (`I`, `you`, `he`, etc.) separate from their verbs.
6.  **NO_SUB TOKEN:** If an English word has no direct equivalent (like the auxiliary 'did' in questions), map it to the special token `NO_SUB`.
7.  **NO PUNCTUATION:** Do not include punctuation like commas or periods in your output phrases.
8.  **OUTPUT FORMAT:** For each sentence ID, provide the MAPPINGS, then the VALIDATION section, following the structure below.
9. **Contractions are atomic** English contractions like "I'm", "don't", "it's", "you're" are single semantic units. You MUST map them within a single phrase. DO NOT split them into their constituent parts (e.g., "I" and "'m"). Contractions may be part of larger phrases if deemed necessary for atomic mapping.

*--- VERB PHRASE TRAINING EXAMPLES (for MAPPINGS section) ---**
`was brought -> fue traída`
`was gone -> había desaparecido`
`did not come back -> no volvió`
`set out -> partió`
`happened to him -> le sucedió`
`had to -> tenía que`
`to watch -> vigilar`
`look for it -> buscarlo`
`is going to eat -> va a comer`
`was standing -> estaba de pie`
`was called -> fue convocado`
*------------------------------------**

**-- Contractions --**
I'm -> estoy
don't -> no
it's -> es
he's -> es
you're -> eres
*-------------------**

**--- FULL SENTENCE EXAMPLES ---**

**EXAMPLE 1:**
USER INPUT:
S1: A certain king had a beautiful garden

YOUR OUTPUT:
S1:
MAPPINGS:
A -> Un
certain -> cierto
king -> rey
had -> tenía
a -> un
beautiful -> hermoso
garden -> jardín
VALIDATION: A certain king had a beautiful garden

**EXAMPLE 2:**
USER INPUT:
S2: He had to take a walk

YOUR OUTPUT:
S2:
MAPPINGS:
He -> Él
had to -> Tenía que
take a walk -> dar un paseo
VALIDATION: He had to take a walk

**EXAMPLE 3:**
USER INPUT:
S3: Why did you run so fast

YOUR OUTPUT:
S3:
MAPPINGS:
Why -> Por qué
did -> NO_SUB
you -> tú
run -> corriste
so -> tan
fast -> rápido
VALIDATION: Why did you run so fast


---


USER INPUT:
