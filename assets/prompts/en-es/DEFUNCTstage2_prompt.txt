You are an expert literary Spanish linguist. Your task is to perform a strict token-for-token lemmatization on a batch of Spanish sentences.

For each input sentence, you must provide a corresponding line of Spanish lemmas with the same ID number.

**CRITICAL: Lemmatization Rules**
1.  **Strict Token-for-Token Alignment:** The input will be treated as a series of space-separated tokens. Your output MUST contain the exact same number of space-separated tokens as the input.
2.  **Lemmatize Standard Words:** For each token that is a standard word, provide its lemma (e.g., "corrió" -> "correr"; "casas" -> "casa").
3.  **Use `_IGNORE_` for Non-Words:** For any token that is a proper noun (e.g., "Alicia"), punctuation (e.g., "¡", "?", ","), or a number, you MUST output the special token `_IGNORE_`.
4.  **Handle Attached Punctuation:** If a token has attached punctuation (e.g., "saltó."), treat the entire token as a single unit. Lemmatize the word part. The output must still be a single token. For example, "saltó." -> "saltar".
5.  **Reflexive Verbs:** For reflexive verbs (e.g., "sentarse"), the output should be the single infinitive form ("sentar").
6.  **Low Confidence:** If you are unsure about the correct lemma for a word, make your best guess. Do not leave it blank or fail to produce an output token.
7.  **No Commentary:** Do not include explanations or notes in your output. Your entire response must consist only of lines in the `id XXX: ...` format.

---
**Example 1: Basic Sentence**
Input: `id 001: El rápido zorro marrón saltó.`
Output: `id 001: el rápido zorro marrón saltar`

**Example 2: With Punctuation and Proper Noun**
Input: `id 002: ¿Las Aventuras de Alicia en el País?`
Output: `id 002: _IGNORE_ el aventura de _IGNORE_ en el país _IGNORE_`

**Example 3: Proper Noun Only**
Input: `id 003: Bingley.`
Output: `id 003: _IGNORE_`

**Example 4: Complex Sentence with Reflexive Verb**
Input: `id 004: Alice se sentía muy cansada, y quería sentarse.`
Output: `id 004: _IGNORE_ se sentir muy cansado _IGNORE_ y querer sentar _IGNORE_`
---

Now, process the following batch of sentences.