You are an expert Spanish linguist. Your task is to perform precise, token-for-token lemmatization on a batch of Spanish phrase segments.

For EACH input segment (ADVANCED_TEXT and SIMPLER_TEXT), you must provide a list of lemmas.

**CRITICAL: Lemmatization Rules**
1.  **Strict Token-for-Token Alignment:** For each line, your output MUST contain the exact same number of space-separated tokens as the input text has words.
2.  **Lemmatize Standard Words:** For standard words, provide the lemma (infinitive verbs; singular, masculine nouns/adjectives).
3.  **Use `_IGNORE_` for Non-Words:** For any token that is a proper noun (e.g., "Alicia"), punctuation (e.g., "¡", "?", ","), or a number, you MUST output the special token `_IGNORE_`.
4.  **Handle Attached Punctuation:** If a token has attached punctuation (e.g., "sentía,"), treat it as a single unit and lemmatize the word part. The output must still be a single token (e.g., "sentía," -> "sentir").
5.  **Reflexive Verbs:** For reflexive verbs (e.g., "sentarse"), the output should be the single infinitive form ("sentar").
6.  **Low Confidence:** If you are unsure about the correct lemma for a word, make your best guess. Do not leave it blank.
7.  **No Commentary:** Your entire response must consist only of the required output lines.

--- EXAMPLE ---

Input:
id 100_A1:
ADVANCED_TEXT: Alicia se sentía muy cansada, y quería sentarse.
SIMPLER_TEXT: Alice se sintió muy cansada y quiso sentar.

Expected Output:
id 100_A1:
ADVANCED_LEMMAS: _IGNORE_ se sentir muy cansado _IGNORE_ y querer sentar _IGNORE_
SIMPLE_LEMMAS: _IGNORE_ se sentir muy cansado y querer sentar _IGNORE_

---
Now, process the following batch of segments.