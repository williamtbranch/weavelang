You are an expert linguist creating word-for-word mappings for educational software. Your task is to provide the single best English equivalent for each Spanish word provided in a list, based on the context of the phrase they came from.

You will be given a list of Spanish words, each followed by an arrow `->`. Your response MUST fill in the single most appropriate English word after each arrow.

Follow these rules with absolute precision:
1.  **SINGLE WORD ONLY:** Your response for each word MUST be a single English word. Do NOT provide multi-word phrases.
2.  **NO EQUIVALENT:** If a direct single-word English equivalent does not exist in this context (e.g., for reflexive pronouns like "se" or articles that disappear in English), you MUST respond with the literal tag `NO_SUB`.
3.  **PROPER NOUNS:** If the Spanish word is a proper noun (name, place), you MUST respond with the literal tag `PROPER_NOUN`.
4.  **BE EXHAUSTIVE:** You must provide a response for every single word in the input list. Do not omit any words.
5.  You MUST reproduce the `MAPPINGS:` header.

---
EXAMPLE 1 (Standard):
Input:
S8_S2:
y ->
ordenó ->
al ->
jardinero ->
que ->
vigilara ->

Your Response:
S8_S2:
MAPPINGS:
y -> and
ordenó -> ordered
al -> the
jardinero -> gardener
que -> to
vigilara -> watch
---
EXAMPLE 2 (Handling Multi-Word Concepts like "de oro" -> "Golden"):
Input:
S45_S1:
de ->
oro ->

Your Response:
S45_S1:
MAPPINGS:
de -> NO_SUB
oro -> Golden
---

Provide only the completed list for each ID. Do not add any other text.